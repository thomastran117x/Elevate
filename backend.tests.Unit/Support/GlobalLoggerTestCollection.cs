namespace backend.tests.Unit.Support;

[CollectionDefinition(Name, DisableParallelization = true)]
public sealed class GlobalLoggerTestCollection
{
    public const string Name = "Global logger tests";
}
